# Audio Model Workspace

Independent audio AI workspace for WavLM Base+ research, fine-tuning, inference, evaluation, and export.

Guardrails:

- Do not train WavLM from scratch.
- Do not commit model weights or datasets.
- Keep audio model code independent from video model code.
- Use local CPU only for small smoke tests and integration checks.

