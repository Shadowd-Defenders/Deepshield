# Exp001 Frozen WavLM Results

Training logs, validation metrics, and held-out test metrics from `exp001_frozen_wavlm` will be saved here.

No metrics are present until the experiment is run on a real dataset split.
