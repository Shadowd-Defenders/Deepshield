# API

Versioned HTTP route handlers will live here.

Planned endpoints:

- `POST /v1/analysis`
- `GET /v1/analysis/{analysis_id}`
- `GET /v1/detectors`
- `GET /v1/analysis/{analysis_id}/artifacts/{artifact_id}`

