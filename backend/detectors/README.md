# Detectors

Detector adapters live here behind a stable internal contract.

Detector folders must not contain training pipelines. Training and experimentation belong in `audio_model/` or `video_model/`.

